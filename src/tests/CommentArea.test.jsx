import { describe, expect, it } from 'vitest'
import { render, screen } from '@testing-library/react'

describe("testing component Comment Area", () => {

    it("checks if comments are shoowing correctly", () => {


    })
})